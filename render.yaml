services:
  - type: web
    name: working-days-system-backend
    env: python
    branch: main
    root: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      # DATABASE_URL must be set in Render for production Postgres.
      # If you use a Render managed database, bind it and expose it via DATABASE_URL.
      - key: DJANGO_ALLOWED_HOSTS
        value: "127.0.0.1,localhost,working-days-system-backend.onrender.com"
      - key: DJANGO_SECRET_KEY
        generate: true
      - key: DJANGO_DEBUG
        value: "false"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://working-days-system-frontend.onrender.com"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://working-days-system-frontend.onrender.com"
      - key: FRONTEND_URL
        value: "https://working-days-system-frontend.onrender.com"

  - type: static
    name: working-days-system-frontend
    env: static
    branch: main
    root: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: "https://working-days-system-backend.onrender.com/api"
